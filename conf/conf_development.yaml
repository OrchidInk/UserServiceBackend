# Database config
database:
  driver: postgres
  url: postgresql://UserService:%s@103.50.205.86:5432/OrchidUserService?sslmode=disable
  password: "User@Service"

# Migration database config
migration:
  dir: file://db/migration

# RSA keys for auth
rsa:
  size: 2048
  public: ./utils/secure/rsa.pub
  private: ./utils/secure/rsa
